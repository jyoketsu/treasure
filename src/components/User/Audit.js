import React, { Component } from 'react';
import './Audit.css';

class Audit extends Component {
    render() {
        return (
            <div className="audit">
                <div className="my-station-head">审核</div>
            </div>
        );
    };
}

export default Audit;